#include <stdio.h>
#include <math.h>

int main() {

  float num = -9.32;

  // num = pow(num, 2);
  // num = sqrt(num);
  // num = round(num);
  // num = floor(num);
  // num = ceil(num);
  // num = abs(num);

  printf("%f", num);

  return 0;
}